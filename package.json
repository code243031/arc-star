{
  "name": "arcstar-network",
  "version": "1.0.0",
  "description": "main-net",
  "main": "build/main.js",
  "scripts": {
    "start": "npm run compile && cross-env DEBUG=true electron .",
    "compile": "tsc && copyfiles -f index.html build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.0.2"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "cross-env": "^7.0.3",
    "electron": "^23.2.1"
  }
}
